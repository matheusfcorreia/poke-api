{
  "name": "poke-api",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Matheus <matheus.freitas@softcenter.com.br>",
  "license": "MIT",
  "scripts": {
    "dev": "docker-compose -f docker/docker-compose.dev.yml up",
    "start": "tsnd -r tsconfig-paths/register --respawn src/server.ts",
    "start:dev": "tsnd -r tsconfig-paths/register --respawn src/server.ts",
    "test": "jest --watch-all"
  },
  "devDependencies": {
    "@types/jest": "^27.5.1",
    "jest": "^28.1.0",
    "nodemon": "^2.0.16",
    "ts-jest": "^28.0.3",
    "ts-node": "^10.8.0",
    "typescript": "^4.7.2"
  }
}
